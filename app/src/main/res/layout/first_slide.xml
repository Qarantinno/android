<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/slide1">
    <ImageView
        android:id="@+id/first_slide_logo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:scaleType="centerCrop"
        android:src="@drawable/question"
        android:layout_marginTop="270dp"
        />
    <TextView
        android:id="@+id/slide1_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/slide1_title"
        android:textColor="@color/textColorBlack"
        android:fontFamily="@font/allura"
        android:textSize="50sp"
        android:paddingTop="20dp"
        android:textStyle="bold"
        android:layout_below="@+id/first_slide_logo"
        android:layout_marginTop="12dp"
        android:gravity="center"/>

    <TextView
        android:id="@+id/slide1_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/slide1_desc"
        android:textColor="@color/textColorBlack"
        android:fontFamily="@font/allura"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_below="@+id/slide1_title"
        android:layout_marginTop="12dp"
        android:gravity="center"/>

</RelativeLayout>